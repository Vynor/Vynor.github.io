```c++
#include <bits/stdc++.h>
#define IOS ios::sync_with_stdio(false);cin.tie(0);cout.tie(0);

using namespace std;

void solve(){

} 

signed main(){
	IOS;
	int t = 1;
//	cin >> t;
	while (t -- ){
		solve(); 
	}
	return 0;
}
```